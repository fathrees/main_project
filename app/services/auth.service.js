(function() {
    "use strict";

    angular.module("app")
        .factory("authService", authService);

    authService.$inject = [];

    function authService() {
        var service = {

        };

        return service;
    }

})();