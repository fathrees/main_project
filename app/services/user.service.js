(function() {
    "use strict";

    angular.module("app.user")
        .factory("userService", userService);

    userService.$inject = [];

    function userService() {
        var service = {

        };

        return service;
    }

})();